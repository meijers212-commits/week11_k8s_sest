python -m venv .venv

.venv\script\activate

pip install -r requirements.txt

cd  streamlit_minikube_app-main

docker build -t elazarmiejers/streamlit-test:v1 .

streamlit run main.py --server.port 8080 --server.address=0.0.0.0

docker run -d --name cc -p 8080:8080 elazarmeijers/streamlit-test:v1

docker push elazarmeijers/streamlit-test:v1 

git add commands.txt

gti commit -m "hgfdsdfghjhgf"

git push -u origin dockerfile

git switch dockerfil 

git add . 

git commit -m "changing dockerfile and commands.txt"

git push

git ckeckout main

git merge dockerfile

git add . 

git commit -m "conniting and miving to pod_yaml branch to work on pod yaml file"

git pull

minikube dashboard

git add . 

git commit -m "adding pod.yaml file"

git checkout main

git merge pod_yaml

 git checkout -b service_yaml

 kubectl apply -f k8s

 minikube service streamlit-service --url 